// Plantilla. Copiá este archivo a `config.js` y completá con tus valores.
// `config.js` está gitignoreado — no se versiona.
//
// SUPABASE_URL y SUPABASE_ANON_KEY: Dashboard → Project Settings → API Keys.
// El anon key es público (lo protege el RLS). NO pongas acá el service_role
// ni la API key de Claude — esos van solo del lado del servidor.
window.EDUTIA_CONFIG = {
  SUPABASE_URL: "https://TU-REF.supabase.co",
  SUPABASE_ANON_KEY: "tu-anon-key-publica",
};

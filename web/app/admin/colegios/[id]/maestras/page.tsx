'use client';
// Stub de Fase 0 (Dashboard admin v3) — lo reemplaza WP2 (Maestras).
import EnConstruccion from '@/components/admin/EnConstruccion';

export default function Page() {
  return <EnConstruccion titulo="Maestras del colegio" />;
}

'use client';
// Stub de Fase 0 (Dashboard admin v3) — lo reemplaza WP3 (Accesos).
import EnConstruccion from '@/components/admin/EnConstruccion';

export default function Page() {
  return <EnConstruccion titulo="Accesos y límites" />;
}

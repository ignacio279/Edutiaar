'use client';
// Stub de fundaciones (fase Observatorio y avisos) — lo reemplaza WP-A.
import EnConstruccion from '@/components/admin/EnConstruccion';

export default function Page() {
  return <EnConstruccion titulo="Observatorio educativo" />;
}

'use client';
// Stub de Fase 0 (Dashboard admin v3) — lo reemplaza WP7 (CRM y alertas).
import EnConstruccion from '@/components/admin/EnConstruccion';

export default function Page() {
  return <EnConstruccion titulo="Alertas" />;
}

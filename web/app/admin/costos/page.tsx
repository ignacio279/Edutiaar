'use client';
// Stub de Fase 0 (Dashboard admin v3) — lo reemplaza WP6 (Costos).
import EnConstruccion from '@/components/admin/EnConstruccion';

export default function Page() {
  return <EnConstruccion titulo="Costos y salud" />;
}

# Secrets — NUNCA commitear
.env
.env.*
!.env.example

# Supabase local
.supabase/
supabase/.temp/

# Dependencias / build
node_modules/
dist/
.vercel/

# OS / editor
.DS_Store
*.log

# Artefactos de test de browser (Playwright MCP)
.playwright-mcp/

# Front: config local con claves (anon es pública pero no la versionamos)
frontend/config.js
